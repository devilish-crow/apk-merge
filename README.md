# apk-merge
A small ash script for managing configuration files generated by the Alpine Package Keeper. Run `curl https://raw.githubusercontent.com/devilish-crow/apk-merge/main/main.sh >> /usr/bin/apk-merge && chmod 744 /usr/bin/apk-merge` as root.
